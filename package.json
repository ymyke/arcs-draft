{
  "name": "arcs-draft",
  "version": "1.0.0",
  "private": true,
  "description": "Draft Arcs leader and lore cards over a shared link.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test \"test/**/*.test.js\"",
    "sync-cards": "node scripts/sync-cards.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
